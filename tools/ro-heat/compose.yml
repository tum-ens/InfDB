services:
    ro-heat:
        build:
            context: .
            dockerfile: Dockerfile
        volumes:
            - ${CONFIG_INFDB_PATH}:/app/mnt/configs-infdb:ro # for infdb configs
            - ./configs:/app/configs:ro # for configs
            - /app/.venv
        extra_hosts:
            - "host.docker.internal:host-gateway"
