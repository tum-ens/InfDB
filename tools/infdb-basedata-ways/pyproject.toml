[project]
name = "infdb-basedata-ways"
version = "1"
requires-python = "==3.12.*"
dependencies = [
    "pyinfdb",
    "geopandas",
    # Add your packages here
]

[tool.uv.sources]
pyinfdb = { path = "/workspaces/infdb_package", editable = true }