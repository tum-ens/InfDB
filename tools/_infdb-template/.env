# Add required environment variables here. for example:
# SERVICE_SPECIFIC_VARIABLE=1234
# IMPORT_ENVIRONMENT_VARIABLE_FROM_GLOBAL_EXPORT=${SERVICES_QWC_POSTGRES_PASSWORD}
UV_LINK_MODE=copy # for development only, because of mount
AGS=${AGS}