# Documentation
This documentation was created with Materials for MkDocs. The content is written in Markdown, a simple markup language.

### Run the Documentation Locally
```bash
uv run --group docs mkdocs serve --livereload -f ./docs/mkdocs/mkdocs.yml -a 0.0.0.0:8000
```